server:
  port: 8081

spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}


security:
  server:
    host: ${EXCHANGE_SERVER_HOST:localhost}
    port: ${EXCHANGE_SERVER_PORT:8080}



complete:
  worker:
    size: ${WORKER_SIZE:5}